# Client log format for structured log ingestion
# Loaded as /etc/nginx/conf.d/01-log-format.conf (before server block)

log_format client_log_format escape=json
    '{"time":"$time_iso8601","remote_addr":"$remote_addr","body":"$request_body","content_length":"$content_length"}';
